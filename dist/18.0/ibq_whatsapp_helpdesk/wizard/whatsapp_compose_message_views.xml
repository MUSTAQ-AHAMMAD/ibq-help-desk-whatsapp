<?xml version="1.0" encoding="utf-8"?>
<!-- GENERATED FOR ODOO 18 - DO NOT EDIT.
     Source: wizard\whatsapp_compose_message_views.xml
     Rebuild with tools/build_v18.py after editing the Odoo 17 file. -->
<odoo>

    <record id="view_whatsapp_compose_message_form" model="ir.ui.view">
        <field name="name">whatsapp.compose.message.form</field>
        <field name="model">whatsapp.compose.message</field>
        <field name="arch" type="xml">
            <form string="Send WhatsApp Message">
                <div class="alert alert-warning" role="alert"
                     invisible="not session_warning or composition_mode == 'template'">
                    <field name="session_warning" nolabel="1" readonly="1"/>
                </div>
                <group>
                    <group>
                        <field name="number"/>
                        <field name="partner_id" readonly="1" invisible="not partner_id"/>
                        <field name="account_id"/>
                    </group>
                    <group>
                        <field name="composition_mode" widget="radio"/>
                        <field name="template_id"
                               invisible="composition_mode != 'template'"
                               required="composition_mode == 'template'"/>
                        <field name="conversation_id" invisible="1"/>
                        <field name="ticket_id" invisible="1"/>
                        <field name="in_session" invisible="1"/>
                    </group>
                </group>
                <field name="body" placeholder="Type your reply..."
                       readonly="composition_mode == 'template'"/>
                <footer>
                    <button name="action_send" type="object" string="Send"
                            class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

</odoo>
