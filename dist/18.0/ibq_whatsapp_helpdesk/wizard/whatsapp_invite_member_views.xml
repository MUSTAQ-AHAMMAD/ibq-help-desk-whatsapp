<?xml version="1.0" encoding="utf-8"?>
<!-- GENERATED FOR ODOO 18 - DO NOT EDIT.
     Source: wizard\whatsapp_invite_member_views.xml
     Rebuild with tools/build_v18.py after editing the Odoo 17 file. -->
<odoo>

    <record id="view_whatsapp_invite_member_form" model="ir.ui.view">
        <field name="name">whatsapp.invite.member.form</field>
        <field name="model">whatsapp.invite.member</field>
        <field name="arch" type="xml">
            <form string="Add Team Members">
                <field name="existing_user_ids" invisible="1"/>
                <group>
                    <field name="user_ids" widget="many2many_tags"
                           options="{'no_create': True}"
                           placeholder="Pick the people who will answer chats…"/>
                </group>
                <group>
                    <group string="Role">
                        <field name="role" widget="radio"/>
                    </group>
                    <group string="Coverage">
                        <field name="team_ids" widget="many2many_tags"
                               placeholder="Every department"/>
                        <field name="account_ids" widget="many2many_tags"
                               placeholder="Every sender"/>
                    </group>
                </group>
                <group>
                    <group string="Workload">
                        <field name="max_active_chats"/>
                        <field name="status"/>
                    </group>
                    <group string="Onboarding">
                        <field name="notify"/>
                    </group>
                </group>
                <div class="alert alert-info" role="alert">
                    Their Odoo access is granted from the role: Agent, Supervisor and
                    Administrator each map to a WhatsApp security group, so there is
                    no second place to keep in sync. Only the Owner can hand out
                    Administrator.
                </div>
                <footer>
                    <button name="action_add" type="object" string="Add to Team"
                            class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_whatsapp_invite_member" model="ir.actions.act_window">
        <field name="name">Add Team Members</field>
        <field name="res_model">whatsapp.invite.member</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
